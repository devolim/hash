# Hash Flutter App - Environment Variables
# Copy this file to .env and fill in your values.
# Then run: flutter run --dart-define-from-file=.env

# Supabase
SUPABASE_URL=https://your-supabase-url.com
SUPABASE_ANON_KEY=your_supabase_anon_key

# Mapbox (public token for map tiles)
MAPBOX_TOKEN=your_mapbox_public_token

# LiveKit (WebSocket URL for voice/video calls)
LIVEKIT_WS_URL=wss://call.yourdomain.com

# Certificate pinning host for call server
CALL_HOST=call.yourdomain.com

# Website URL (for legal pages: privacy, terms, delete-account)
WEBSITE_URL=https://yourdomain.com

# Stripe donation link (optional)
DONATE_URL=https://donate.stripe.com/your_stripe_link
