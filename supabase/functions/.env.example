# Supabase Edge Functions - Environment Variables
# Copy this file to .env and fill in your values.

# LiveKit API key (used by call-token, call-end, call-heartbeat, call-p2p-failed Edge Functions)
LIVEKIT_API_KEY=your_livekit_api_key

# LiveKit API base URL (e.g. https://call.yourdomain.com)
LIVEKIT_API_BASE_URL=https://call.yourdomain.com

# Firebase Service Account JSON (used for push notifications via FCM)
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"your-project-id",...}
